WAScan
$ git clone https://github.com/m4ll0k/WAScan.git
$ cd wascan 
$ pip install BeautifulSoup
$ python wascan.py
If you want the w script to work you'll need to make sure your lib and plugin folder is in the same directory as your w script.

Photon
Python LIBS:
requests
requests[socks]
urllib3
tld

git clone https://github.com/s0md3v/Photon/

nikto is built into kali
